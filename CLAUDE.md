# Instruções para Claude Code

Siga integralmente [AGENTS.md](./AGENTS.md). Backend: seção **Backend hexagonal**. Frontend: seção **Frontend (Next.js)** e `.cursor/rules/frontend.mdc` (MUI, dayjs `DD/MM/YYYY HH:mm:ss`, cor só no dinheiro).

Antes de concluir uma mudança no backend, execute `npm test` e `npm run build` dentro de `backend/` quando o ambiente permitir.
