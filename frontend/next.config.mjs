/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@mui/x-date-pickers', '@mui/system', '@mui/material', '@mui/icons-material'],
};

export default nextConfig;
